import type { Metadata } from 'next'
import { Inter, Playfair_Display } from 'next/font/google'
import { Analytics } from '@vercel/analytics/next'
import './globals.css'

const inter = Inter({ subsets: ["latin"], variable: '--font-inter' });
const playfair = Playfair_Display({ subsets: ["latin"], variable: '--font-playfair' });

export const metadata: Metadata = {
  title: 'VoiceLink - Connecting Voices to Opportunities',
  description: 'Professional voice talent marketplace connecting voice actors with clients worldwide',
  keywords: 'voice actors, voice over, voice talent, hire voice actors, voiceover marketplace',
  generator: 'v0.app',
  openGraph: {
    title: 'VoiceLink - Connecting Voices to Opportunities',
    description: 'Professional voice talent marketplace connecting voice actors with clients worldwide',
    type: 'website',
  },
  icons: {
    icon: [
      {
        url: '/icon-light-32x32.png',
        media: '(prefers-color-scheme: light)',
      },
      {
        url: '/icon-dark-32x32.png',
        media: '(prefers-color-scheme: dark)',
      },
    ],
  },
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="es" className="dark">
      <body className={`${inter.variable} ${playfair.variable} font-sans antialiased`}>
        {children}
        <Analytics />
      </body>
    </html>
  )
}
